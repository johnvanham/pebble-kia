# Drop this block into the existing Caddyfile on the Pi. Caddy's
# automatic Let's Encrypt TLS covers the subdomain as soon as DNS
# points at the Pi.

kia-proxy.example.com {
    encode zstd gzip
    reverse_proxy 127.0.0.1:8000
}
